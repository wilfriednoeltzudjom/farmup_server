const addressSchema = require('./address.schema');
const assetSchema = require('./asset.schema');
const categorySchema = require('./category.schema');

module.exports = { addressSchema, assetSchema, categorySchema };
